{
  "name": "bear-bash-matchmaking-server",
  "version": "1.0.0",
  "description": "A UDP and HTTP based matchmaking server",
  "main": "server.ts",
  "type": "module",
  "scripts": {
    "start": "node build/server.js",
    "start:dev": "nodemon --exec ts-node server.ts",
    "build": "tsc",
    "clean": "rimraf ./build",
    "lint": "eslint . --ext .ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.5",
    "dgram": "^1.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "eslint": "^8.22.0",
    "nodemon": "^2.0.15",
    "rimraf": "^3.0.2",
    "ts-node": "^10.8.0",
    "typescript": "^4.6.3"
  }
}
